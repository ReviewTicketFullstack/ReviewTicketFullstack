# AC-52 / ARCH-R4 — java-core main 소스에 나타나면 안 되는 문자열.
#
# 이 목록이 .java 가 아니라 이 파일에 있는 이유:
# 테스트 소스에 이 단어들을 적으면, 소스 전체를 훑는 가드레일 GR-03 이
# 테스트 파일 자체를 위반으로 잡는다. 검사 대상과 검사 규칙을 같은 확장자에
# 두지 않는 것으로 그 자기참조를 피한다.
#
# sdk/tools/check-boundaries.sh 의 GR-03 패턴과 같은 뜻을 담는다. 한쪽을
# 고치면 다른 쪽도 고쳐야 한다 — 의도적인 이중화다(문서 검사와 빌드 검사).
#
# 대소문자는 무시하고 비교한다. 빈 줄과 # 로 시작하는 줄은 건너뛴다.
dinov2
facebook/
transformers
clip-vit
torch.

/**
 * [PROTOTYPE] 초기 스캐폴딩 단계의 모델 정의.
 * 현재는 @/shared/types 를 사용하고 있습니다.
 *
 * 이 디렉터리는 백업 목적으로만 유지되며, 신규 기능 개발 시 참조하지 마세요.
 * 특히 Order 모델은 구조가 다르므로 (items: OrderItem[]) 혼동하지 않도록 주의하세요.
 */

export type { Store } from './store';
export type { MenuItem } from './menu';
export type { Order, OrderItem } from './order';
export type { Review } from './review';
